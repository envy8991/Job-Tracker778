{
  "scripts": {
    "test:firebase-rules": "vitest run firebase-emulator-tests"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.8.1",
    "vitest": "^3.1.4"
  }
}
